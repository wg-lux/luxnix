{
  "task_status": "complete",
  "findings": [
    {
      "claim": "Receiver transfer locations require ssl_crl and a runtime freshness gate. Bootstrap requires authenticated CRL publication before Nginx startup; a separate 60-second refresh timer reloads Nginx and closes transfers on refresh/reload failure. TLS resumption is disabled for the receiver virtual host and transfer locations limit keepalive to one request.",
      "source": "modules/nixos/services/lx-annotate-local/config.nix; modules/nixos/services/lx-annotate-local/options/hub.nix",
      "confidence": "high"
    },
    {
      "claim": "Publisher requires pinned HTTPS publisher authentication, refuses redirects and credential-bearing URLs, validates complete direct CRLs signed by every pinned client CA, enforces time bounds and CRL-number rollback protection, and publishes atomically without replacing the prior bundle on validation failure.",
      "source": "scripts/vault/publish-hub-crl.py",
      "confidence": "high"
    },
    {
      "claim": "uv run pytest -q tests/test_hub_client_revocation.py: exit 0, 10 passed in 1.28s. Real disposable Nginx accepted the old certificate before revocation, rejected it after CRL publication and reload, accepted its replacement, rejected unknown publisher TLS CA, and returned 503 after gate removal. Other cases cover incomplete chains, wrong signatures, future/stale/expired CRLs, rollback and insecure endpoints.",
      "source": "tests/test_hub_client_revocation.py; local run 2026-09-07",
      "confidence": "high"
    },
    {
      "claim": "uv run ruff check scripts/vault/publish-hub-crl.py tests/test_hub_client_revocation.py; nix-instantiate --parse modules/nixos/services/lx-annotate-local/config.nix; nix-instantiate --parse modules/nixos/services/lx-annotate-local/options/hub.nix; git diff --check: all exit 0. tracker.py validate: exit 0, 10 valid features.",
      "source": "Local verification 2026-09-07",
      "confidence": "high"
    },
    {
      "claim": "Official Nginx documentation requires intermediate CRLs in the same PEM file as the leaf issuer CRL; Vault exposes public CRL endpoints, whose transport is authenticated here without a bearer token.",
      "source": "https://nginx.org/en/docs/http/ngx_http_ssl_module.html#ssl_crl; https://developer.hashicorp.com/vault/api-docs/secret/pki",
      "confidence": "high"
    }
  ],
  "gaps": [
    "No live hosts contacted or production certificates revoked. Controlled receiver rejection and measured propagation/recovery time remain operator acceptance requirements.",
    "Root integration must run full Nix evaluation with the untracked helper included in its source snapshot and integrate operator documentation. Individual Nix parse checks do not establish systemd boot/reload behavior.",
    "Initial rollout requires existing independently pinned Vault TLS CA, client CA bundle and an available direct HTTPS CRL endpoint before Nginx can start. Custom/intermediate CA deployments must supply one CRL URL per issuer; missing issuer CRLs fail closed.",
    "CRL age defaults to 72 hours to match Vault defaults. Custodians should configure a shorter Vault CRL expiry and automatic rebuild then lower crlMaxAgeSeconds; the timer polls every 60 seconds, and any attempted refresh failure closes new transfer requests immediately. If the timer is stopped or disabled, OpenSSL CRL nextUpdate expiry remains the hard bound.",
    "Graceful Nginx reload does not terminate already-running uploads or other existing requests. Immediate breach containment still needs trusted receiver/network actions, followed by rejection tests. Shared browser sessions lose TLS resumption; only transfer locations lose HTTP keepalive.",
    "Nginx is reloaded after publication; successful systemctl reload does not itself prove application acceptance. Runtime tests cover real Nginx CRL behavior, not a complete NixOS VM boot or failure-injected systemd reload.",
    "First disposable Nginx attempt failed because ambient LD_LIBRARY_PATH mixed incompatible Nix glibc versions. The isolated test subprocess now removes LD_LIBRARY_PATH and LD_PRELOAD; rerun passes with real Nginx 1.28.0."
  ]
}
